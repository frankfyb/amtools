# 密码生成器开发指南

## 🚀 快速开始

本指南将带您一步步实现一个功能完整的密码生成器工具。适合 React 和 Next.js 初学者。

## 📋 开发步骤

### 步骤 1：创建工具页面文件

**创建文件**：`src/app/tools/password-generator/page.tsx`

```typescript
'use client'

import { useState } from 'react'

export default function PasswordGenerator() {
  return (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-2xl mx-auto px-4">
        <h1 className="text-3xl font-bold text-center mb-8">密码生成器</h1>
        {/* 这里将添加更多内容 */}
      </div>
    </div>
  )
}
```

### 步骤 2：添加状态管理

```typescript
'use client'

import { useState } from 'react'

export default function PasswordGenerator() {
  // 密码配置状态
  const [config, setConfig] = useState({
    length: 12,
    includeUppercase: true,
    includeLowercase: true,
    includeNumbers: true,
    includeSymbols: true,
  })
  
  // 生成的密码
  const [password, setPassword] = useState('')
  
  // 复制状态
  const [copied, setCopied] = useState(false)

  return (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-2xl mx-auto px-4">
        <h1 className="text-3xl font-bold text-center mb-8">密码生成器</h1>
        {/* UI 组件将在这里添加 */}
      </div>
    </div>
  )
}
```

### 步骤 3：实现密码生成逻辑

```typescript
// 生成密码的函数
const generatePassword = () => {
  let charset = ''
  
  if (config.includeUppercase) charset += 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'
  if (config.includeLowercase) charset += 'abcdefghijklmnopqrstuvwxyz'
  if (config.includeNumbers) charset += '0123456789'
  if (config.includeSymbols) charset += '!@#$%^&*()_+-=[]{}|;:,.<>?'
  
  if (charset === '') {
    alert('请至少选择一种字符类型')
    return
  }
  
  let result = ''
  for (let i = 0; i < config.length; i++) {
    result += charset.charAt(Math.floor(Math.random() * charset.length))
  }
  
  setPassword(result)
  setCopied(false) // 重置复制状态
}
```

### 步骤 4：添加复制功能

```typescript
// 复制到剪贴板的函数
const copyToClipboard = async () => {
  if (!password) return
  
  try {
    await navigator.clipboard.writeText(password)
    setCopied(true)
    setTimeout(() => setCopied(false), 2000) // 2秒后重置状态
  } catch (err) {
    console.error('复制失败:', err)
  }
}
```

### 步骤 5：计算密码强度

```typescript
// 计算密码强度的函数
const getPasswordStrength = (pwd: string) => {
  if (!pwd) return { level: 0, text: '无' }
  
  let score = 0
  
  // 长度评分
  if (pwd.length >= 8) score += 1
  if (pwd.length >= 12) score += 1
  if (pwd.length >= 16) score += 1
  
  // 字符类型评分
  if (/[a-z]/.test(pwd)) score += 1
  if (/[A-Z]/.test(pwd)) score += 1
  if (/[0-9]/.test(pwd)) score += 1
  if (/[^a-zA-Z0-9]/.test(pwd)) score += 1
  
  if (score <= 2) return { level: 1, text: '弱' }
  if (score <= 4) return { level: 2, text: '中' }
  return { level: 3, text: '强' }
}
```

### 步骤 6：完整的 UI 组件

```typescript
'use client'

import { useState } from 'react'

export default function PasswordGenerator() {
  const [config, setConfig] = useState({
    length: 12,
    includeUppercase: true,
    includeLowercase: true,
    includeNumbers: true,
    includeSymbols: true,
  })
  
  const [password, setPassword] = useState('')
  const [copied, setCopied] = useState(false)

  const generatePassword = () => {
    let charset = ''
    
    if (config.includeUppercase) charset += 'ABCDEFGHIJKLMNOPQRSTUVWXYZ'
    if (config.includeLowercase) charset += 'abcdefghijklmnopqrstuvwxyz'
    if (config.includeNumbers) charset += '0123456789'
    if (config.includeSymbols) charset += '!@#$%^&*()_+-=[]{}|;:,.<>?'
    
    if (charset === '') {
      alert('请至少选择一种字符类型')
      return
    }
    
    let result = ''
    for (let i = 0; i < config.length; i++) {
      result += charset.charAt(Math.floor(Math.random() * charset.length))
    }
    
    setPassword(result)
    setCopied(false)
  }

  const copyToClipboard = async () => {
    if (!password) return
    
    try {
      await navigator.clipboard.writeText(password)
      setCopied(true)
      setTimeout(() => setCopied(false), 2000)
    } catch (err) {
      console.error('复制失败:', err)
    }
  }

  const getPasswordStrength = (pwd: string) => {
    if (!pwd) return { level: 0, text: '无' }
    
    let score = 0
    
    if (pwd.length >= 8) score += 1
    if (pwd.length >= 12) score += 1
    if (pwd.length >= 16) score += 1
    
    if (/[a-z]/.test(pwd)) score += 1
    if (/[A-Z]/.test(pwd)) score += 1
    if (/[0-9]/.test(pwd)) score += 1
    if (/[^a-zA-Z0-9]/.test(pwd)) score += 1
    
    if (score <= 2) return { level: 1, text: '弱' }
    if (score <= 4) return { level: 2, text: '中' }
    return { level: 3, text: '强' }
  }

  const strength = getPasswordStrength(password)

  return (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-2xl mx-auto px-4">
        <h1 className="text-3xl font-bold text-center mb-8">密码生成器</h1>
        
        <div className="bg-white rounded-lg shadow-md p-6">
          {/* 密码长度设置 */}
          <div className="mb-6">
            <label className="block text-sm font-medium text-gray-700 mb-2">
              密码长度: {config.length}
            </label>
            <input
              type="range"
              min="4"
              max="50"
              value={config.length}
              onChange={(e) => setConfig({...config, length: parseInt(e.target.value)})}
              className="w-full h-2 bg-gray-200 rounded-lg appearance-none cursor-pointer"
            />
            <div className="flex justify-between text-xs text-gray-500 mt-1">
              <span>4</span>
              <span>50</span>
            </div>
          </div>

          {/* 字符类型选择 */}
          <div className="mb-6">
            <label className="block text-sm font-medium text-gray-700 mb-3">
              包含字符类型:
            </label>
            <div className="grid grid-cols-2 gap-3">
              <label className="flex items-center">
                <input
                  type="checkbox"
                  checked={config.includeUppercase}
                  onChange={(e) => setConfig({...config, includeUppercase: e.target.checked})}
                  className="mr-2"
                />
                大写字母 (A-Z)
              </label>
              <label className="flex items-center">
                <input
                  type="checkbox"
                  checked={config.includeLowercase}
                  onChange={(e) => setConfig({...config, includeLowercase: e.target.checked})}
                  className="mr-2"
                />
                小写字母 (a-z)
              </label>
              <label className="flex items-center">
                <input
                  type="checkbox"
                  checked={config.includeNumbers}
                  onChange={(e) => setConfig({...config, includeNumbers: e.target.checked})}
                  className="mr-2"
                />
                数字 (0-9)
              </label>
              <label className="flex items-center">
                <input
                  type="checkbox"
                  checked={config.includeSymbols}
                  onChange={(e) => setConfig({...config, includeSymbols: e.target.checked})}
                  className="mr-2"
                />
                特殊符号 (!@#$...)
              </label>
            </div>
          </div>

          {/* 生成按钮 */}
          <div className="mb-6">
            <button
              onClick={generatePassword}
              className="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 px-4 rounded-lg transition-colors"
            >
              生成密码
            </button>
          </div>

          {/* 密码显示区域 */}
          {password && (
            <div className="mb-6">
              <label className="block text-sm font-medium text-gray-700 mb-2">
                生成的密码:
              </label>
              <div className="flex">
                <input
                  type="text"
                  value={password}
                  readOnly
                  className="flex-1 px-3 py-2 border border-gray-300 rounded-l-lg bg-gray-50 font-mono text-sm"
                />
                <button
                  onClick={copyToClipboard}
                  className={`px-4 py-2 rounded-r-lg font-medium transition-colors ${
                    copied 
                      ? 'bg-green-600 text-white' 
                      : 'bg-gray-600 hover:bg-gray-700 text-white'
                  }`}
                >
                  {copied ? '已复制' : '复制'}
                </button>
              </div>
            </div>
          )}

          {/* 密码强度指示器 */}
          {password && (
            <div className="mb-4">
              <div className="flex items-center justify-between mb-2">
                <span className="text-sm font-medium text-gray-700">密码强度:</span>
                <span className={`text-sm font-medium ${
                  strength.level === 1 ? 'text-red-600' :
                  strength.level === 2 ? 'text-yellow-600' : 'text-green-600'
                }`}>
                  {strength.text}
                </span>
              </div>
              <div className="w-full bg-gray-200 rounded-full h-2">
                <div
                  className={`h-2 rounded-full transition-all duration-300 ${
                    strength.level === 1 ? 'bg-red-500 w-1/3' :
                    strength.level === 2 ? 'bg-yellow-500 w-2/3' : 'bg-green-500 w-full'
                  }`}
                />
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  )
}
```

### 步骤 7：集成到首页导航

**修改文件**：`src/app/page.tsx`

在 `toolsData` 数组中添加密码生成器：

```typescript
// 找到合适的分类，比如"生产力工具"
{
  id: 'password-generator',
  name: '密码生成器',
  description: '生成安全的随机密码，支持自定义长度和字符类型',
  url: '/tools/password-generator', // 注意这是内部路由
  category: '生产力工具' // 根据您的分类调整
}
```

## 🎯 关键知识点

### 1. React Hooks
- **useState**：管理组件状态
- **useEffect**：处理副作用（如果需要）

### 2. 事件处理
- **onChange**：处理输入变化
- **onClick**：处理按钮点击

### 3. 条件渲染
- 使用 `&&` 操作符进行条件渲染
- 使用三元操作符处理不同状态

### 4. 数组和对象操作
- 展开操作符 `...` 更新状态
- 对象属性的动态更新

## 🔧 调试技巧

### 1. 控制台调试
```typescript
console.log('当前配置:', config)
console.log('生成的密码:', password)
```

### 2. 状态检查
在开发过程中，可以临时显示状态：
```typescript
<pre>{JSON.stringify(config, null, 2)}</pre>
```

### 3. 错误处理
```typescript
try {
  // 可能出错的代码
} catch (error) {
  console.error('错误:', error)
}
```

## 🎨 样式说明

### 1. Tailwind CSS 类名
- `bg-gray-50`：浅灰色背景
- `rounded-lg`：圆角边框
- `shadow-md`：中等阴影
- `hover:bg-blue-700`：悬停效果

### 2. 响应式设计
- `max-w-2xl`：最大宽度限制
- `grid-cols-2`：两列网格布局
- `flex`：弹性布局

## 🚀 扩展建议

### 1. 功能扩展
- 添加密码历史记录
- 支持自定义字符集
- 添加密码安全性检查

### 2. UI 改进
- 添加动画效果
- 支持深色模式
- 改进移动端体验

### 3. 性能优化
- 使用 useMemo 缓存计算结果
- 防抖处理用户输入

这个指南提供了完整的实现步骤和代码示例，您可以按照步骤逐步实现，每完成一步都可以测试功能是否正常工作。