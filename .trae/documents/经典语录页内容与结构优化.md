## 概述
- 优化 `/src/app/tools/classic-quotes` 的数据来源、页面结构与交互动画，统一使用已有 `lib` 与 `types`，减少重复逻辑并增强离线回退与可访问性。

## 当前问题
- 重复数据拉取：页面本身拉取（src/app/tools/classic-quotes/page.tsx:30-51）与 Hook（src/app/tools/classic-quotes/hooks/useFeishuQuotes.ts:21-38）并存，状态维护重复。
- 缺失导入与潜在错误：`Link` 未导入（page.tsx:160）；直接引用 `gsap.core.Tween`（page.tsx:26、93）但未导入 `gsap`，可能导致运行时/类型错误。
- 回退常量未定义：`QUOTES` 未定义（page.tsx:62），导致无远端数据时可能报错。
- 组件未复用：页面内重复实现导航、信息栏与动画，已有组件（components/Toolbar.tsx、StatusBar.tsx、QuoteCard.tsx）未被使用。

## 目标
- 统一数据源为 `useFeishuQuotes` 与 API（src/app/api/feishu/classic-quotes/route.ts），并与 `src/lib/feishu/ClassicQuotesService.ts`、`src/types/feishu.ts` 协同。
- 引入稳定的本地回退数据：使用 `classic-quotes.json` 的 `quotes` 字段作为后备。
- 复用组件、统一动画入口、提升可访问性与移动端体验。

## 改动概要
- 数据流整合：只保留 `useFeishuQuotes` 管理远端与刷新；删除页面内的 `fetchFeishu` 与 `remoteQuotes`。
- 回退数据：静态导入 `./classic-quotes.json` 的 `quotes`，映射为 `Quote[]`，作为 `fallbackQuotes`。
- 页面结构：在 `page.tsx` 中使用 `Toolbar`、`QuoteCard`、`StatusBar`，移除重复 DOM 与 GSAP 逻辑。
- 交互控制：添加轻量控制区（顶部工具栏右侧）管理 `auto` 开关与 `anim` 模式（fade/slide/scale/type），将动画执行权交由 `QuoteCard`。
- 修复导入：显式导入 `next/link` 的 `Link`（或改由 `Toolbar` 内使用 `Link`），移除页面内对 `gsap` 的直接引用。

## 详细实现步骤
1. 修改 `page.tsx`（src/app/tools/classic-quotes/page.tsx:13）：
   - 删除 `fetchFeishu` 与 `remoteQuotes` 状态（30-51、28），保留 `useFeishuQuotes`（56-58）。
   - 新增静态回退：从 `./classic-quotes.json` 读取 `quotes`，映射到 `Quote[]`，形成 `fallbackQuotes`。
   - `filteredQuotes = quotes || fallbackQuotes`（替换原 `filtered`，修复 `QUOTES` 未定义问题，62）。
   - 将卡片展示与动画交由 `QuoteCard`；通过 props 传递 `q`、`loading/slow/err`、`onFetch/onRefreshLatest/onToggleFav/isFav/anim`。
   - 将顶部导航替换为 `<Toolbar onToggleFavView={...}/>`；底部信息栏使用 `<StatusBar status={api} updatedAt={updatedAt} />`。
   - 显式导入 `Link`（若继续在页面内使用）；移除页面中 `gsap` 类型与动画逻辑（87-123、129-152）。
2. 保持组件实现：
   - `QuoteCard.tsx` 已完整处理 GSAP 动画与可访问性（src/app/tools/classic-quotes/components/QuoteCard.tsx:25-60、62-87、90-127）。
   - `Toolbar.tsx` 与 `StatusBar.tsx` 保持接口不变；如需 `Link`，考虑在 `Toolbar` 内改为使用 `next/link`（非必需）。
3. 数据类型与服务：延用 `Quote`、`Payload`（src/types/feishu.ts:1-11）；后端 `ClassicQuotesService` 负责规范化（src/lib/feishu/ClassicQuotesService.ts:70-99）与 ETag（105-110）。
4. 自动播放与节能：
   - `auto` 开启时 `setInterval(fetchQuote, 5000)`；在页面 `visibilitychange` 为 hidden 时暂停（由 `QuoteCard` 已处理摇摆动画的节能，62-87）。
5. 错误处理与提示：保留现有错误文案与“重试”逻辑；无远端时使用回退数据确保功能可用。

## 验证
- 正常网络：远端数据加载、刷新（调用 `refresh`）后随机展示；收藏持久化与收藏页正常。
- 断网/错误：显示错误提示，重试可恢复；仍可使用本地 `classic-quotes.json` 的 `quotes`。
- 动画与自动播放：切换 `anim` 生效；`auto` 周期切换语录且在页面不可见时暂停摇摆动画。
- 兼容与无障碍：按钮 `aria-label`、`aria-live`、焦点环正常；移动端布局稳定。

## 交付
- 更新 `page.tsx`，接入组件与本地回退、修复导入与重复逻辑；保留 Hook 与后端服务/类型的既有结构。

## 风险与回滚
- 若组件化后样式有偏差，可暂保留原类名与布局，逐步迁移；如出现功能异常，回滚到当前 `page.tsx` 版本即可。